mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        video(src=`/${video.videoUrl}` controls) 
        p=video.description
        small=video.createdAt
        br
        small Created by 
            a(href=`/users/${video.owner.id}`)=video.owner.username
        each hashtag in video.hashtags
            p=hashtag
                
            
        hr