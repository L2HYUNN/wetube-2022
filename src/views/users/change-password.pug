extends ../base

block content
  if errorMessage
    span=errorMessage
  form(method="post")
    input(name="currentPassword" placeholder="Current Password" required)
    input(name="newPassword" placeholder="New Password" required)
    input(name="newPasswordConfimation" placeholder="New Password Confirmation" required)
    input(type="submit" value="Change Password")